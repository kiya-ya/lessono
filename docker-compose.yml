name: sister-report

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sister-report
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
