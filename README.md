# 姐妹团数据统计报表

> 兔玩大数据平台姐妹团模块的数据统计与可视化报表系统，用于追踪 2026-07-17 新政策上线前后的关键指标变化。

---

## 功能特性

| 模块 | 功能 |
|------|------|
| 📊 工作台 | 厅体检卡墙（红黄绿健康度+迷你趋势线）、预警横幅（可点击直达问题厅）、KPI 大/小卡、4 张趋势图（含平台均值参考线） |
| 🏠 权限视角 | 厅运营只看自己管理的厅；管理员看厅排行榜（真实厅总流水口径+位次变化）；按角色自动过滤 |
| 📈 核心趋势 | 留存率折线、解散率叠加图、流水柱状图、任务堆叠图、日级本周vs上周叠加 |
| 🔍 对比分析 | 厅四象限散点图（流水×留存）、政策前后对比表、大厅排名、双轴图 |
| 📐 政策评估 | 政策前4周vs后4周均值对比、分厅响应度发散条形图+明细表 |
| 👑 姐姐分析 | 姐姐画像、妹妹晋升追踪、姐姐排行榜（当日奖励口径）、头牌依赖度警示、**培养力候选池**（四因子并列展示 + 倾斜动作结果记录） |
| 🔔 预警中心 | 历史预警列表、未处理计数、标记已处理/恢复 |
| 📋 明细数据 | 姐妹团存活分析（天数分布+7/14/30日存活率+政策前后）、搜索/排序/分页/状态筛选 |
| 👤 UID查询 | 本周vs上周对比、姐妹团参与信息、累计流水（server1 合计）、姐姐vs妹妹对比图、CSV导出 |
| 🍪 Cookie管理 | 双Cookie分别管理，更新后立即生效（热加载） |
| 📱 手机适配 | 侧栏折叠为顶部横条，全页面响应式 |
| 🎨 界面 | 侧边栏布局（可折叠）、靛蓝单品牌色降饱和配色、粒子交互登录页 |

---

## 技术栈

- **后端**: Python 3.11 + Flask + SQLite
- **前端**: 纯 HTML + JavaScript + ECharts 5（模块化：config / api / app / render / workbench / insight / auth）
- **数据抓取**: requests + BeautifulSoup4
- **部署**: Docker / Windows 本地

---

## 部署方式

### 方式一：Windows 本地部署（推荐）

**前提**: 已安装 Python 3.11+

```bash
cd D:\姐妹团看板系统
deploy.bat
```

脚本自动完成：检查 Python → 安装依赖 → 初始化数据库 → 启动服务。

访问: `http://127.0.0.1:5000`

外部访问：`http://192.168.100.19:5000`

---

### 方式二：Docker 部署

**前提**: 已安装 Docker Desktop

```bash
cd D:\姐妹团看板系统

# 构建镜像
docker-compose build --no-cache

# 后台启动
docker-compose up -d

# 查看日志
docker-compose logs -f
```

访问: `http://127.0.0.1:5000`

#### 手机/局域网访问

1. 查看电脑局域网 IP：`ipconfig`，找 `192.168.x.x`
2. 确保手机和电脑连接**同一 WiFi**
3. 手机浏览器访问 `http://192.168.x.x:5000`
4. 如无法访问，检查 Windows 防火墙是否放行 5000 端口

#### 常用命令

| 命令 | 作用 |
|------|------|
| `docker-compose up -d` | 后台启动 |
| `docker-compose down` | 停止并删除容器 |
| `docker-compose ps` | 查看运行状态 |
| `docker-compose restart` | 重启服务 |

---

## 首次使用

### 1. 更新 Cookie

点击页面右上角 **🍪 Cookie 管理**，分别粘贴：
- **UID 查询 Cookie**: 从 `server1.tuwan.com:10010` 页面复制
- **数据抓取 Cookie**: 从 `bigdata.tuwan.com` 页面复制

Cookie 格式要求包含 `PHPSESSID`。

### 2. 抓取数据

首次使用数据库为空，需要手动抓取历史数据：

```bash
# 在项目目录执行
python daily_crawl.py
```

或配置 Windows 定时任务自动每日更新：
```bash
python setup_task.py
```

### 3. 使用 UID 查询

- 在 **👤 UID查询** 标签页输入 UID
- 勾选「模拟数据模式」可无需内网测试前端
- 关闭模拟模式需确保已更新有效 Cookie 且连接内网

---

## 项目结构

```
姐妹团看板系统/
├── backend/
│   └── app.py              # Flask API 主入口（所有接口）
├── crawler/
│   ├── crawler.py          # bigdata 数据抓取（含大厅日流水）
│   ├── db.py               # 数据库操作
│   ├── uid_crawler.py      # UID查询抓取（Cookie 热加载）
│   ├── metrics.py          # 指标计算
│   ├── alerts.py           # 预警引擎
│   └── hall_manager_crawler.py  # 厅运营UID抓取
├── data/
│   ├── stats.db            # SQLite 主数据库（唯一使用中的库）
│   ├── cookie.json         # UID查询Cookie
│   ├── cookie_bigdata.json # 数据抓取Cookie
│   └── exports/            # 导出文件
├── frontend/
│   ├── index.html          # 主页面（侧边栏布局）
│   ├── login.html          # 登录页（交互粒子背景）
│   ├── assets/             # LOGO 等静态资源
│   ├── css/style.css       # 样式（含设计令牌 --wb-*）
│   ├── js/config.js        # 全局配置
│   ├── js/api.js           # API 请求封装
│   ├── js/app.js           # 页面交互（标签切换/侧栏/刷新）
│   ├── js/render.js        # 核心趋势/对比分析图表
│   ├── js/workbench.js     # 工作台（卡墙/排行榜/KPI/趋势/四象限/日级叠加/存活）
│   ├── js/insight.js       # 政策评估/姐姐分析/预警中心
│   └── js/auth.js          # 登录状态与侧栏用户信息
├── docs/
│   ├── README.md           # 文档导航（现行 vs 归档）
│   └── archive/            # 历史设计文档（PRD/进度/完成度快照）
├── tests/                  # 实用脚本
│   ├── verify_data_accuracy.py   # 数据准确性交叉验证（46项）
│   ├── backfill_hall_revenue.py  # 厅流水历史回填（一次性）
│   └── make_logo.py              # LOGO 主题色生成
├── daily_crawl.py          # 定时抓取入口（含预警检测）
├── deploy.bat              # Windows一键部署
├── requirements.txt        # Python依赖
└── 项目上下文记忆.md        # 会话上下文与排障记录
```

---

## 数据更新策略

| 数据 | 更新频率 | 方式 |
|------|---------|------|
| 统计数据 | T+1 | `daily_crawl.py` 定时抓取 |
| 基础数据 | T+1 | 随统计数据一起更新 |
| UID数据 | 按需 | 手动查询或批量查询 |
| Cookie | 按需 | 前端粘贴更新 |

---

## 核心指标计算说明

| 图表 | 数据来源 | 计算公式 |
|------|---------|---------|
| 💯 留存率趋势 | `weekly_report` 表 | (周末进行中 - 本周新成团) / 周始进行中 × 100% |
| 🚫 解散率趋势 | `weekly_report` 表 | 本周解散数 / ((周始进行中 + 周末进行中) / 2) × 100% |
| 💰 礼物流水趋势 | `weekly_report` 表 | 本周7天 `reward_amount` 累计（全部大厅总和） |
| 📊 任务活跃度 | `weekly_report` 表 | (开车任务 + 陪档任务 + 收送礼任务) / 周末进行中 |

**术语说明：**
- **周始进行中**：本周第一天（周一）正在进行中的姐妹团数量
- **周末进行中**：本周最后一天（周日）正在进行中的姐妹团数量
- **本周新成团**：本周内新成立的姐妹团数量
- **本周解散数**：本周内解散的姐妹团数量

---

## 版本历史

### v1.6.0 (2026-08-14) — 培养力候选池 + 结果记录

- ✅ 姐姐分析新增「候选池」子页（画像 / 候选池 分段切换）：并列展示有培养力潜力的姐姐及其四因子证据——留存（存活率）、妹妹成长（月升级数）、共同成长、牌子等级，**不做权威排序**
- ✅ 候选判定口径：存活率≥50% 且 妹妹有成长 且 牌子≥铜牌；权重暂为「留存 = 妹妹成长 > 共同成长 > 牌子等级（门槛）」，待攒出成功案例后演进为权威排序（见 docs/adr/0001）
- ✅ 结果记录：可对候选姐姐回填「输送妹妹 / 提拔管理」动作（日期/类型/备注），并补填后续结果（妹妹是否毕业 / 团是否存活 / 流水是否增长 + 总体判断），形成「动作→结果」闭环用于攒成功案例
- ✅ 新增后端接口 `/api/talent-pool`、`/api/talent-actions`、`/api/talent-actions/<id>/result`，及 `talent_actions` 表（启动自动创建、自动补列）
- ✅ 新增术语表 `CONTEXT.md`（姐妹团领域词汇）+ 架构决策 `docs/adr/`（B→A 演进路径）

### v1.5.0 (2026-08-13) — 姐妹团数据强化

- ✅ 厅排行榜新增「进行中姐妹团」「姐妹团周流水（礼物奖励口径）」两列，与厅总流水并列展示
- ✅ 核心趋势页并入工作台：删除重复的 4 张周级图，日级本周vs上周叠加图移入工作台趋势区，导航减为 7 项
- ✅ 对比分析：四象限散点图替换为「厅留存率分布」直方图（分段计数，简单直观）
- ✅ 各大厅对比柱状图点击后：明细表和存活分析都按点击的大厅过滤（联动大厅筛选器）
- ✅ 姐姐排行榜：支持 当日/当周/当月 奖励周期切换（周/月为跨快照去重累计），表头点击排序
- ✅ 头牌依赖度警示过滤掉当日奖励 <500 元的小厅（避免单人厅必然 100% 的噪音）

### v1.4.2 (2026-08-13) — Cookie 保活

- ✅ Cookie 自动保活：后台线程每 30 分钟用已存 Cookie 访问一次两个系统（server1 / bigdata），防止 PHP session 因不活跃过期——Cookie 长期有效，无需手动更新
- ✅ Cookie 管理面板显示保活状态（🫀 保活正常 · 时间 / ⚠️ 失败提醒）
- ✅ 每日抓取任务同步执行一次保活
- ✅ 排查并解决端口冲突：Docker 容器 `sister-report`（旧代码镜像）一直占用 5000 端口与本地 python 进程随机响应请求，已停止（如需用 Docker 部署，需重建镜像并只保留一种运行方式）
- ✅ UID 查询调试文件改为 `UID_DEBUG=1` 开关控制，默认不再生成

### v1.4.1 (2026-08-13) — 交互细节与项目清理

- ✅ 登录页：恢复可交互粒子背景（鼠标牵引+邻近连线高亮），LOGO 换成点点开黑吉祥物（背景色随主题，脚本 `tests/make_logo.py`）
- ✅ 侧边栏：`«` 按钮折叠/展开，主内容动态居中（展开时在剩余区域居中，收起时窗口居中），状态记忆
- ✅ 侧栏用户信息简化：UID + 中文身份徽章 + 管理的厅名 + 退出登录
- ✅ 数据库：users 表启动时同步厅名（hall_name 列），新增 `users_simple` 视图（UID/管理的厅名/身份）
- ✅ 项目清理：删除一次性修复脚本/调试文件/重复虚拟环境，历史文档移入 docs/，tests/ 仅保留 3 个实用脚本

### v1.4.0 (2026-08-12) — 视觉改版

- ✅ 侧边栏布局：横向标签页改为左侧深色固定侧栏（品牌 + 8 个导航 + 底部 Cookie/用户区），顶部工具条保留时间/大厅筛选；手机端侧栏折叠为顶部横滑条
- ✅ 配色收敛：全站单一品牌色靛蓝 `#4F5BD5`，语义色统一降饱和（绿 `#3D9A6C` / 红 `#D56060` / 琥珀 `#C98A2D`），全部图表同步换色
- ✅ 登录页重写：深墨底 + CSS 光晕网格（去掉粒子动画）、靛蓝 logomark、玻璃登录卡、纯色登录按钮

### v1.3.3 (2026-08-12) — 厅总流水口径接入

- ✅ 厅排行榜/四象限的"周流水"改为**真实厅总流水**口径：新增 `hall_revenue_daily` 表，每日抓取 bigdata「大厅主题」页（/roomnew/indextest/4，全厅日流水）；已用厅详情页回填近 29 天历史（`tests/backfill_hall_revenue.py`）
- ✅ 数据验证：王者荣耀·特别的人 2026-08-03~08-09 厅总流水 338,955.50，与 bigdata 查询完全一致
- ✅ 周覆盖不足 7 天的厅流水在排行榜中标注天数；无厅流水数据时自动回退姐妹团奖励口径
- ✅ 每日定时任务新增「大厅日流水」抓取步骤（失败不影响主流程）

### v1.3.2 (2026-08-12) — 数据口径修正

- ✅ 姐姐分析流水口径修正：`reward_amount` 实为**快照当日发放的礼物奖励**（单日量级几十~几百元），并非累计总流水；页面已改标「当日奖励」并注明口径，累计总流水请在「UID查询」查看（server1 系统，量级数万）
- ✅ 快照重复行修复：`team_detail` 一天多次抓取会产生重复行（如 08-11 快照 2000 行/1007 团），导致明细表重复展示、统计虚高。爬虫写入前现在先删除当天旧快照（幂等），所有 team_detail 查询统一按 `team_id` 去重（防御历史脏数据）
- ✅ 涉及接口：明细表 / 日级事件 / 存活分析 / 姐姐分析（含头牌依赖度）/ hall_stats 聚合
- ✅ 验证脚本同步更新去重口径，46 项交叉验证全部通过

### v1.3.1 (2026-08-12) — 修复与验证

- ✅ 姐姐分析：「团长分析」更名为「姐姐分析」（页面文案统一为姐姐）
- ✅ UID 查询 Cookie 热加载：每次查询实时读取 `data/cookie.json`，页面更新 Cookie 后立即生效，无需重启后端（此前必须重启才生效）
- ✅ 数据准确性交叉验证：新增 `tests/verify_data_accuracy.py`，对 KPI / 大厅概览 / 存活分析 / 日级事件 / 政策评估 / 姐姐排行共 46 项指标做 API vs SQL 独立重算比对，全部通过

### v1.3.0 (2026-08-12) — 运营闭环（第三期）

- ✅ 政策评估专项页：政策前4周 vs 政策后4周均值对比卡（留存+6.6pp、解散-5.6pp、流水+46.8%、新团+32.2%），分厅响应度发散条形图（改善/恶化各TOP10）+ 明细表
- ✅ 团长（姐姐）分析页：团长排行榜（带团数/进行中/团存活率/累计流水），头牌依赖度警示（TOP1 流水占比 ≥40% 红 / ≥30% 黄）
- ✅ 预警中心页：alerts 表历史列表（高/中/低徽章）、未处理计数、标记已处理/恢复（持久化 is_resolved）
- ✅ 新增接口：`/api/policy-impact`、`/api/captains`、`/api/alerts-center`、`POST /api/alerts/<id>/resolve`

### v1.2.0 (2026-08-12) — 管理决策增强（第二期）

- ✅ 厅四象限分析（对比分析页）：流水×留存率散点图，明星/潜力/风险/衰退四象限，中位数分界，点击散点切换该厅
- ✅ 平台均值参考线：工作台留存率/解散率趋势图，选中具体厅时叠加平台均值虚线（我的厅 vs 大盘水位）
- ✅ 日级同期叠加（核心趋势页）：本周 vs 上周新成团/解散按星期对齐双线，基于 team_detail 快照的 form_date/dissolve_date
- ✅ 姐妹团存活分析（明细数据页）：进行中团天数分布直方图 + 7/14/30日存活率 + 政策前后 7 日存活对比
- ✅ 新增接口：`/api/daily-events`（日级成团/解散）、`/api/survival`（存活分析）

### v1.1.0 (2026-08-12) — 工作台改版（第一期）

- ✅ 全新「工作台」首页：厅体检卡墙（🔴需关注 / 🟡有波动 / 🟢健康，含 7 周迷你趋势线），点卡联动全页数据
- ✅ 预警横幅：按真实触发原因展示（留存骤降/流水骤降/连续无新团），点击直达问题厅
- ✅ 管理员视角：厅排行榜（周流水/留存率排名 + 较上周位次变化）
- ✅ 视觉改版：墨蓝黑导航条 + 语义化涨跌色 + 等宽数字；整体 1200px 居中；手机端响应式
- ✅ 登录与权限：UID 白名单 + 验证码登录；厅运营（hall_manager）仅见自己管理的厅
- ✅ 新增 `/api/hall-overview` 聚合接口；`/api/kpi` 支持按厅过滤，所选周无数据时自动回退最新一周
- ✅ 体验细节：记住上次选择的大厅和周（localStorage）、数据新鲜度色点（>24h 黄 / >48h 红）
- ✅ 修复：切换大厅图表不刷新（图表实例未销毁）；UID 查询遇特殊符号昵称时 GBK 编码崩溃（接口 500）
- ✅ 工具栏移除「导出CSV / 导出PDF」入口（接口保留，明细页和 UID 页导出不变）

### v1.0.2 (2026-08-06)

- ✅ 姐妹团累计流水：数据来源改为 `server1.tuwan.com:10010`，计算方式为姐姐+妹妹累计总流水合计
- ✅ UID查询：查询妹妹UID时，自动追加姐姐的累计流水，确保团合计准确
- ✅ 明细数据：默认按最新快照日期**倒序排列**（最新在前）
- ✅ 明细数据：删除"姐妹团累计流水"列（数据来源不一致，改为在UID查询中查看）
- ✅ 留存率/解散率趋势：修复日期重复问题（SQL按日期聚合去重）
- ✅ 前端模块化：拆分为 config.js / api.js / app.js / render.js
- ✅ 测试文件整理：所有临时脚本移至 `tests/` 目录，提供 `clean_tests.bat` 一键清理
- ✅ .gitignore：排除敏感数据文件（cookie.json、cookie_bigdata.json 等）

### v1.0.1 (2026-07-31)

- ✅ 明细数据搜索支持大厅名称筛选（搜索框输入大厅名即可过滤）
- ✅ UID查询新增姐姐vs妹妹对比分析图表
- ✅ 姐姐vs妹妹柱状图使用独立Y轴，避免数值差异过大

### v1.0.0 (2026-07-31)

- ✅ 周报概览：KPI卡片 + 趋势图 + 预警列表
- ✅ 核心趋势：4图联动 + 时间截断 + 政策分界线
- ✅ 对比分析：政策前后对比 + 大厅排名 Top10 + 双轴图
- ✅ 明细数据：搜索/分页/状态筛选/UID点击跳转
- ✅ UID查询：本周vs上周 + 姐妹团信息 + CSV导出
- ✅ Cookie管理：双Cookie分别管理
- ✅ 数据导出：周报CSV、明细CSV、周报PDF（含图表+预警）
- ✅ 预警系统：自动生成5条规则
- ✅ Docker部署支持 + Windows一键启动
- ✅ 定时任务：每日23:30自动抓取

- ✅ 周报概览：KPI卡片 + 趋势图 + 预警列表
- ✅ 手机响应式：基础适配
- ⚠️ 手机响应式：基础适配

---

## 常见问题

**Q: 如何启动服务？**
A: 两种方式：
- 开发启动：`.venv\Scripts\python.exe start.py`（自动打开浏览器）
- 生产部署：双击 `deploy.bat`

**Q: 如何按大厅筛选数据？**
A: 两种方式：
1. **顶部工具栏**的「全部大厅」下拉框 → 控制**整个页面**的数据范围（KPI、趋势图、明细表等）
2. **明细搜索框**输入大厅名称 → 只过滤**明细表格**的数据

**Q: 如何清理临时调试文件？**
A: 双击运行项目根目录的 `clean_tests.bat`，自动清理 `tests/` 目录下的所有临时文件。

**Q: 页面空白怎么办？**
A: 按 `Ctrl+F5` 强制刷新清除缓存；检查后端是否启动；F12 控制台查看 JS 错误。

**Q: UID查询显示"Cookie过期"？**
A: 打开 UID查询页面 → F12 → Network → 复制 Cookie → 粘贴到 Cookie管理 → 保存。

**Q: 数据不更新？**
A: 检查 `data/last_update.json` 确认上次更新时间；运行 `python daily_crawl.py` 手动抓取；或配置定时任务 `python setup_task.py`。

**Q: 启动报 `ModuleNotFoundError`？**
A: 确保使用 `.venv\Scripts\python.exe` 运行，而不是系统 Python。虚拟环境已包含所有依赖。

---

> 项目路径: `D:\姐妹团看板系统`
> 当前版本: `v1.4.x`（详见上方版本历史）
