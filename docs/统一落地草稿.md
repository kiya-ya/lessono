# 统一落地草稿（预览定稿后执行）

> 临时工作草稿，落地完成后归档进 `docs/archive/`。不并入 README 版本历史，仅作本次统一落地的执行蓝图。

## 0. 状态速览

| 层 | 状态 |
|---|---|
| 后端（口径 + 预警接口） | ✅ 已全部落地并提交（`0448ab1` 口径、`2c3b4ca` 妹妹成长分/留存说明） |
| 预览（`preview-visual.html`） | ✅ 5 页齐全：概览 / 预警中心 / 厅分析 / 姐姐分析 / UID查询，含「团队明细 → 姐姐/妹妹 UID 跳转」「存活分析直方图 ≤30 天」「厅分析/姐姐分析页头时间选择器」 |
| 前端正式代码（`frontend/*`） | ⬜ 未落地 —— 本稿即落地蓝图 |

后端口径项（第一~三项）已收口；本轮主体仍是前端落地 + 一处新交互（UID 跳转），但新增一处后端口径修正：**已成团天数（已解散团改算 `dissolve_date − form_date`，见 I 节）**。

## 1. 落地总原则

- 逐页定稿后**一次性**编写正式代码，不再零敲碎打。
- 每改一个 JS/CSS 文件，用 `?v=N` 升版号做缓存失效（`index.html` 的 `<script>`/`<link>`）。
- 每个改动 commit 到 master（遵守既有约定）。
- 预览是唯一蓝图：字段文案、口径、图表形态都照 `preview-visual.html` 复刻，不另起炉灶。

## 2. 前端落地清单（按依赖顺序）

### A. 预警中心前端落地（后端 `/api/warncenter` 已就绪，纯前端）

1. `index.html` 侧栏加「预警中心」tab，对应新建 `#view-warncenter` 容器（复用现有 `switchTab` 机制）。
2. 新建 `frontend/js/warncenter.js`，接 `GET /api/warncenter`，渲染 4 卡：
   - 留存率卡（`metric_note` = 期末/期初快照口径）
   - 异常解散卡（`ACTIVE_DISS_REASON_SQL`，非毕业解散 4 类集合）
   - 新成团卡
   - 解散时间分布卡（非毕业解散按「成团→解散」存续天数分 4 周直方图；原「任务活跃度/陪档活跃率」卡已删、活跃率并入下方躺平模块 KPI）
3. 概览页新增「平台级预警 4 卡」区块（顶部横幅），停用 KPI 显示 ⚠️ 死徽标。
4. 解散原因饼图配色补「毕业」分支、`detail-reason` 下拉同步「毕业/离职(换厅)/注销」选项。**新增两条系统解散原因（本轮定稿）**：生态违规（假车/买卖流水/一人多号/内部挖人/涉黄）、等级同时低于铜牌——后端 `DISSOLVE_REASON_CASE`（app.py:208）当前二者都落入 `ELSE '其他'`，落地补 `WHEN dissolve_reason LIKE '%违规%' THEN '生态违规'`、`WHEN dissolve_reason LIKE '%铜牌%' THEN '等级低于铜牌'` 两分支（放在「任务未完成」之后、「毕业」之前，注意别被更宽的 `LIKE` 提前命中）；饼图/下拉/README 已按 8 类口径（毕业 / 手动 / 离职含换厅 / 注销 / 任务未完成 / 生态违规 / 等级低于铜牌 / 其他）。
5. `index.html` 引入 `warncenter.js?v=1`。
6. 每卡「受影响厅」列表加「查看全部受影响厅 →」全量弹窗（复用 `openFull`，默认 Top 2 厅 + 「其余 N 厅合计」备注，点开完整厅排行）；预警中心页头加「时间」周选择器（与「大厅」并列，右侧周期提示）。见 K 节。
7. **解散时间分布后端**（新增）：非毕业解散团按 `dissolve_date − form_date` 存续天数分 4 桶（第1周1-7天 / 第2周8-14天 / 第3周15-21天 / 第4周22-29天），复用 I 节「已解散团存续天数 = dissolve_date − form_date」口径；`/api/warncenter` 返回分桶序列（或独立 `/api/dissolve-dist`）。持续率的分子 SUM(实际存续天数) 与此同源，一次 SQL 可同时出「持续率分母的存续天数」和「分桶直方图」。

### B. 厅分析落地

- `render.js` 的 `_hfCharts` 并入全局 `charts`，修复窗口 resize 时图表 0×0 不重绘。
- `/api/hall-stats` 路由去重已在后端完成（`0448ab1`），前端无需改，但要确认调用的是去重后的返回。
- 厅排行榜 `total_revenue` 口径定稿「**周增量**」：现 `api_hall_stats` 读 `hall_stats.total_revenue`（妹妹**累计**流水，app.py:2101-2107；crawler.aggregate_hall_stats 每日预聚合的累计值），与 KPI/周报「本周流水」口径冲突。改按厅从 `team_detail` 重算周增量流水（= 各厅期末快照 `sister_revenue` 合计 − 期初快照合计，同 `week_metrics_from_detail.weekly_revenue` 口径）；用单次聚合 SQL 实现（仿 `retention_by_hall`，避免 178 厅逐厅调用卡顿），`hall_stats` 表不再作为厅排行流水源。计数列（team_count/active_count/dissolved_count）保持快照口径不变。

### C. 姐姐分析落地

- `insight.js` 画像表补「姐妹关系持续率」列（预览里已体现，当前正式代码未展示），带团毕业率降为副行。**持续率口径定稿**：姐妹关系持续率 = Σ(已结束团实际存续天数) ÷ (已结束团数 × 30) × 100%；毕业团按 30 天 clamp（快照滞后会虚高 31/32）、解散团按 `dissolve_date − form_date` 实际天数、**进行中团不进分子分母**；数学上 = 毕业率 + Σ(解散团存续比例)/已结束团数，天然 ≥ 毕业率。**带团毕业率（辅助）** = 自然毕业团数 ÷ 已结束团数 × 100%（自然毕业团 = `dissolve_reason='毕业'`；已结束团 = 自然毕业团 + 非毕业解散团；进行中团不进；分母 0→空）。后端 `api_sister_profile.retention`（app.py:1002）与 `api_talent_pool.retention`（app.py:1194）**改为持续率**：分子=SUM(已结束团实际存续天数，毕业团 clamp 30)、分母=COUNT(`dissolve_date` 非空团)×30；此 retention 同时进入候选硬门槛（≥65，app.py:1200）与培养力总分（`0.4×姐妹关系持续率`）。**注意**：旧口径（进行中/总数 或 进行中/(总数−毕业)）分子封顶 2、值随带团数趋 0，本次改「自然毕业/已结束」根治；持续率天然 ≥ 毕业率，故「毕业率≥50%」门槛改为「持续率≥65%」（**已定稿 2026-08-31**：实测全量口径 45%、已结束团≥2 口径 38% 的姐姐达标，覆盖率合理）。
- 清理死数据：`/api/captains` 返回的 `dependency`（头牌依赖度）、`survival_rate` 前端不再消费 → 后端删除该字段，前端同步去掉引用。
- `/api/captains` 周/月口径 `active_count` 子查询漏厅过滤（app.py:1418-1419）：`(SELECT COUNT(*) FROM latest l WHERE l.sister_uid = s.sister_uid AND ...)` 未带 `hall_cond`，单厅时 active_count 是全厅口径、与 team_count/total_reward 不自洽；落地补 `hall_cond`（`survival_rate` 虽为死数据待删，但 active_count 修正前先别删，避免别处消费到错误值）。
- 妹妹成长 / 培养升牌率文案口径修正：妹妹成长 = **难度加权成长分/月**（按团 `妹妹最高等级难度分 Δ ÷ 团龄` 只对有成长的团取平均（÷ 有成长团数、不含没升级的团）、再 ×30，其中 Δ = 该团末次快照难度分 − 首次快照难度分（MAX−MIN，末期−初期）；难度分 铜牌2→银牌8→金牌16→王牌32→大神64 指数递增）；培养升牌率 = **妹妹成长覆盖率**（「妹妹有成长（MAX−MIN>0，妹妹至少升过一级）」团数 ÷ 历史带团总数 ×100%，非打分；**去掉「团存活」条件**——一个姐姐最多同时 2 个团，若保留「团进行中」则分子封顶 2、分母随历史递增，值必然趋 0，资深姐姐被系统性低估，与带团毕业率「进行中封顶 2」同类病）。`insight.js:479`、`index.html:498` 已改，落地时核对无遗漏。**名词改名**：指标原名「共同成长」→「培养成功率」→「培养升牌率」（= 妹妹成长覆盖率，有成长团÷历史团）；预览 / README / 本稿已改，落地时同步改前端显示文案（`frontend/index.html:498`、`frontend/js/insight.js:463/479`）与后端注释（`app.py:1130/1163` 的 docstring/注释）。**后端实现 bug**：`api_talent_pool` 培养升牌率的「团存活」判定用了 `MAX(CASE WHEN dissolve_date IS NULL/'' THEN 1 ELSE 0 END)`（app.py:1169），按 team_id 分组时等于「该团曾否存活过」，已解散团因解散前快照 dissolve_date 为空而被误判存活 → 培养升牌率过估；**落地直接删掉 alive 判定**（`joint` 计数不再带 `AND alive` 条件），而非改「最新快照无解散日期」——后者会把分子锁死 ≤2、引入「值随带团数趋 0」的新 bug。
- 培养力象限图改「只显示候选 + 密集区聚合 + 按厅筛选」：默认只画候选（三条全过）、未入选隐藏；单点显示姓名、密集格聚合为「N 位」气泡（点开展开名单）；单点与聚合名单里的姐姐均可点击跳 UID 查询；顶部加「按厅筛选」下拉。候选坐标（妹妹成长/培养升牌率）与 `hall_name`、`sister_uid` 若 `/api/captains` 已返回则纯前端即可，否则后端补。
- 姐姐总览 / 姐姐筛选 / 各全量弹窗表头加「↑/↓ 排序箭头」：通用点击排序（数字/百分比/金额/日期/文本），「操作」列不排序。
- 候选池表加「培养力总分」列：`0.4×姐妹关系持续率 + 0.35×妹妹成长(封顶映射到0-100) + 0.25×培养升牌率`，满分100；仅作排序参考、不进候选硬门槛，权重后台可调。**妹妹成长归一化口径定稿「封顶映射 T=20」**：`min(成长分/月, 20)/20×100`（T=20 为满分锚点：每月 20 成长分记满分。**2026-08-31 已按定稿口径实测定稿**：分母=有成长的团时，成长分/月分布 P50=5.8 / P75=8.2 / P90=14.5 / P95≈22 / P99=43.6——T=20 时头部约 7% 触顶、中腰部有梯度；曾评 T=10（基于旧分母口径分布）在新口径下会致 21% 触顶、头部区分度不足，弃用；原占位 30 经实测几乎无人触顶，弃用。**注意**：与持续率公式的 30（毕业线天数、业务规则）只是数字巧合，含义无关）。三指标 `retention`/`sister_growth`/`joint_growth` 由 `/api/talent-pool` 返回（非 `/api/captains`，后者只返回死字段 `survival_rate`）→ 前端纯计算即可。**后端改动**：妹妹成长分母由 `g['teams']` 改 `g['grew']`（app.py:1187，只对有成长的团取平均）；改口径后数值普遍变大，归一化封顶已按新口径实测分布定稿 T=20（见上）。
- 候选判定牌子门槛定稿「**银牌及以上**」：后端 `api_talent_pool` 的 `candidate` 条件 `level_rank >= 2` 改 `>= 4`（app.py:1200，银牌 rank=4，排除铜牌）；预览与 README 已按银牌口径，落地时后端一并改。

### D. UID 查询落地

- `#uid-type` 下拉由硬编码改为接 `GET /api/uid-query/types`（后端接口已存在，前端 `index.html:520` 目前写死 4 项）。
- 导出 `exportUIDResult`（`render.js:676`）的「累计流水」字段口径修正：`total_revenue` 实际取的是姐姐本周流水，需与参与明细「妹妹累计流水」对齐命名，避免 0 值误导。
- 妹 vs 姐对比在妹妹爬虫缺失时回退本地流水（预览已标，落地接上）。
- 姐 vs 妹对比图改为「组合双轴图」（近 4 周，成团≤30天≈4周）：**一个姐姐最多同时带 2 个妹妹（1姐2妹，多团多妹）**，双轴图并列展示 姐姐/妹妹A/妹妹B 三组柱 + 两条差额折线（姐−妹A、姐−妹B）；差额多线不做零轴大块填充（重叠易乱）、保留零轴虚线 + 负值红点标「反超」；图下自动洞察（分别对两个妹妹）；顶部「指标切换」seg（礼物流水/礼物数/陪档时长/陪档天数），差额单位随指标（¥/个/h/天）。后端 `_get_bound_sisters` 已返回姐姐的所有绑定妹妹，前端按该列表渲染多妹妹（无需新接口）。
- 累计对比表：删「差值」列，保留数字表格（领先值淡绿加粗高亮），列 = 指标 / 姐姐 / 妹妹A / 妹妹B（1姐最多2妹并列），维度 = 本周流水 / 本周礼物数 / 累计流水 / 陪档时长 / 陪档天数 / 当前等级 / 任务完成率。

### E. 新需求：团队明细 → 姐姐/妹妹 UID 跳转 UID 查询 ⭐

> 本轮新增，预览已定稿：点团ID进团队明细后，能看到**姐姐和妹妹的 UID**，且各自可点击跳转到 UID 查询页查看该人完整数据。

1. `api.js` `openTeamDetail`（团详情弹窗）：姐姐/妹妹 UID 由纯文本 `member-uid` 改为可点击链接，`onclick` 调 `jumpToUID(uid)`；**妹妹 UID 也要有跳转入口**（当前只有姐姐的 `tdGoUID` 按钮）。
2. `insight.js` `openSisterDetail`（姐姐下钻带团明细）：团ID 列改为可点进团详情；「妹妹」列补妹妹 UID 可点击跳转。
3. UID 页「参与明细」表（`render.js`）：妹妹 UID 列改为可点击，跳转查妹妹 UID。
4. `jumpToUID` 已存在（`app.js:194`），复用即可，无需新增全局函数。
5. 姐姐总览表（画像+排行）与姐姐筛选表（候选池）各补「姐姐UID」列（数据已含于 `/api/captains` 的 `sister_uid`，纯前端）；UID 单元格可点击跳 UID 查询（复用 `jumpToUID`）。

### F. 死接口 / 死数据清理

- 删 `sister2-profile` / `sister2-detail` 死接口（后端）及前端对应调用。
- 删 `/api/captains` 的 `dependency` / `survival_rate` 死数据字段。
- 明细导出 `/api/export/detail` 的 `total_revenue` 字段实为 `sister_revenue`（妹妹流水），导出表头改「妹妹累计流水」口径，与 UID 页一致。

### G. 时间选择器（周）落地 ⭐ 本轮新增

> 概览页已有「时间」下拉；厅分析、姐姐分析同属「周维度」分析，补同款周选择器。**预警中心本轮也补「大厅 + 时间」双筛选**（原判「当前快照实时状态不加」已推翻——预警中心 4 卡本身就是「近 8 周」趋势，需要选周看历史某周）。UID 查询是「当前用户」实时查询，仍不加。

1. **厅分析**：`index.html` 厅分析页头加「时间」周选择器；`render.js` 读取所选周并重拉 `/api/weekly-report`（当前写死本周 vs 上周，需支持选周）。
2. **姐姐分析**：`index.html` 姐姐分析页头加「时间」周选择器；`insight.js` 读取所选周并重拉 `/api/captains`（头牌/风险打标当前默认本周流水）。
3. **预警中心**：`index.html` 预警中心页头加「时间」周选择器（与「大厅」并列，右侧周期提示随切换更新）；`warncenter.js` 读取所选周并重拉 `/api/warncenter?week=xxx`（4 卡 + 各「受影响厅」排行一起刷新）。预览已定稿（`cc05b3b`）。
4. 后端需确认是否已支持 `week`/`week_start` 过滤参数，不支持则补（`/api/weekly-report`、`/api/captains`、`/api/warncenter`）。
5. 与概览页 `time-select` 的周口径（周一~周日）保持一致。

### H. 存活分析直方图 ≤30 天（确认项，无落地动作）

- 姐妹团满 30 天毕业，已成团天数不可能超 30。后端 `/api/survival` bin 已是 `0-3 / 4-7 / 8-14 / 15-30 / 30天以上`，前端 `workbench.js` 已过滤「30天以上」段——正式代码已正确，仅预览此前 bin 画到 43 天+，本轮已把预览对齐为 4 档 `0-3 / 4-7 / 8-14 / 15-30`。

### I. 已成团天数口径修正（本轮新发现，改后端查询）⭐

> 现象：08-12 成立 / 08-16 解散的团，明细「已成团天数」显示 6，实际应为 4。
> 根因：`days_since_formed` 是爬虫从 bigdata「已成团天数」列原样照抄的「成团第 N 天」（含头 +1），且已解散团会在快照里残留一两天、天数继续涨到快照日（08-17 显示 6）。

1. 已解散团（`dissolve_date` 非空）：`已成团天数` 改为 `dissolve_date − form_date`（真实存活天数），不再用 raw `days_since_formed`。
2. 进行中团：沿用 bigdata 实时值（「第 N 天」口径，与 bigdata 一致）。
3. 覆盖所有展示 `days_since_formed` 的位置：明细搜索表（`/api/detail`）、姐姐画像带团明细（`/api/sister-detail`）、存活分析、团详情弹窗；**并连带修正计算位**——`week_metrics_from_detail.avg_days`（app.py:311）与 `api_talent_pool` 妹妹成长的团龄分母 `MAX(days_since_formed)`（app.py:1168）同样用了 raw 脏值（已解散团改 `dissolve_date − form_date`、进行中团用真实天数），落地一并改，否则「平均成团天数」「成长分/月」被拉偏。
4. 附带确认（非 bug，不改）：同一妹妹可被反复配对（团解散后原班人马重新成团、新团 ID）；妹妹中途改昵称是正常业务。可选优化——姐姐画像「妹妹」列加 UID，避免同名/改名被误读成多个人。

### J. 毕业妹妹：时间范围切换 + 名单富字段 ⭐ 本轮新增

> 预览已定稿（`905304c`）。毕业妹妹卡原写死「最近一周」，改为「今天 / 一周 / 一个月」三档；名单列从「# / 妹妹 / 姐姐 / 毕业日期」扩为富字段。

1. **后端 `/api/captains` 的 `recent_graduates` 查询扩展**（现状只 SELECT 妹妹UID/昵称/大厅/毕业日期/姐姐UID/昵称，`date(dissolve_date) >= date('now','-7 day')` 写死、`LIMIT 15`）：
   - 新增请求参数 `grad_range`（`today` / `week` / `month`，缺省 `week`），WHERE 窗口改为：today=`date('now')`、week=`>= date('now','-7 day')`、month=`>= date('now','-30 day')`；`LIMIT` 去掉或放大（全量名单）。
   - SELECT 补两列：`sister_max_level2`（→ 妹妹最高牌子，`LEVEL_NAMES` 映射）、`sister_revenue`（→ 妹妹累计流水）。
   - **口径：「妹妹最高牌子」取 `sister_max_level2`**（妹妹巅峰成就，不受降级抹除），非 `sister_level2`（当前等级）。姐姐侧不展示牌子列。
2. **前端 `insight.js` `renderRecentGraduates()`**：读 `grad_range` 渲染三档 seg 切换；小预览（TOP3）与全量弹窗列同步扩展（妹妹UID / 姐姐UID / 大厅 / 妹妹最高牌子 / 流水 / 毕业日期）；UID 单元格可点击跳 UID 查询。
3. `index.html` 毕业妹妹卡加 seg 三档按钮，`?v=` 升版号。

### K. 搜索框分层 + 受影响厅全量查看 ⭐ 本轮新增

> 预览已定稿：全站搜索框分「页面级 / 表格级」两层、作用域不重叠；预警中心 4 卡与躺平「大厅分布」的受影响厅列表从 Top N 示例升级为「查看全部 → 全量弹窗」。

1. **搜索框分层口径**（已写进 README FAQ，前端落地照此实现）：
   - **页面级**（每页顶部整条搜索栏）：决定本页「看哪几个厅 / 哪段时间」，改它整页图表一起刷新。概览=类型/组/大厅/时间；预警中心=大厅+时间。
   - **表格级**（某张表上方小输入框）：只过滤紧挨着的那一张表，按团ID/姐姐/妹妹/大厅名定位行。
   - **UID查询输入框** = 查一人的入口，独立于两层。
   - 规则：先页面级圈范围 → 再表格级找行。
2. **受影响厅全量弹窗**：预警中心 4 卡 + 躺平「大厅分布」各加「查看全部受影响厅 →」按钮，复用 `openFull` 全量弹窗（与躺平名单/毕业妹妹名单同款）；默认展示 Top 2~6 厅 + 「其余 N 厅合计」备注，点开看完整厅排行。落地走 `/api/warncenter?metric=xxx`、`/api/lying-flat?group=hall` 返回全量（当前预览为演示节选 Top 8~12）。

## 3. 验证清单

1. 重启 Flask，逐 tab 过 5 页，布局/图表/文案口径与预览一致。
2. 核心链路：点团ID → 团队明细 → 点姐姐 UID / 妹妹 UID → 跳 UID 查询页并自动带出该人数据。
3. 预警中心 4 卡渲染真实数据（非 mock）。
4. 浏览器 console 无报错、无 404 死接口。
5. 全量过一遍后 commit 到 master。
6. 已成团天数：抽验已解散团（如 56224/56225）显示 4 天而非 6；进行中团与 bigdata 一致。
7. 毕业妹妹：三档时间切换（今天/一周/一月）过滤正确；全量名单含妹妹UID/姐姐UID/大厅/妹妹最高牌子/流水；妹妹最高牌子 = `sister_max_level2`。
8. 姐姐总览 / 姐姐筛选 / 毕业妹妹 各表的 UID 可点击跳 UID 查询并自动带出该人数据。
9. 培养力象限：默认只显示候选（未入选隐藏）；按厅筛选生效；密集区聚合为「N 位」气泡、点开能展开该格名单；单点/聚合名单点姐姐跳 UID。
10. 表头排序：各表头可点击排序（数字/百分比/金额/日期/文本），↑/↓ 指示方向；培养升牌率以 % 展示、妹妹成长以成长分/月展示。

## 4. 落地顺序建议

A（预警中心）→ I（已成团天数口径）→ E（UID 跳转，小改动、验收明确）→ B（厅分析）→ C（姐姐分析）→ D（UID 页）→ F（清理收尾）。
A 与 I、E 可并行；F 放最后，避免删除接口时影响前面仍在联调的功能。J（毕业妹妹时间范围+富字段）属姐姐分析，与 C 一并落地。K（搜索框分层 / 受影响厅全量弹窗）为纯前端，随 A、G 一并落地。
