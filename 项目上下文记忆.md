# 姐妹团看板系统 — 项目上下文记忆文档

> 生成时间：2026-08-12 · 最近更新：2026-09-01
> 项目路径：`D:\姐妹团看板系统`
> 当前分支：`main`（注意不是 master）
> Git最新提交：见「九、近期Git提交记录」

---

## 一、项目概述

**姐妹团数据统计报表** — 面向「点点开黑」平台运营团队的Web数据看板，追踪姐妹团（姐姐+妹妹配对）的成团、留存、解散、流水等核心指标，支持政策上线效果追踪。

### 技术栈
- **后端**：Python 3 + Flask + SQLite
- **前端**：原生 HTML/JS + ECharts（本地文件）
- **数据抓取**：Python爬虫（BeautifulSoup + Requests）
- **部署**：本地运行 / Docker
- **认证**：Cookie-based（auth_uid）+ 验证码登录

### 运行方式（重要）
- 必须用项目虚拟环境启动：`.venv/Scripts/python.exe backend/app.py`
- ❌ 不要用 kimi-desktop 自带的 python（缺 flask 模块）
- 日志输出到 `logs/flask.log`
- **代码修改后必须重启后端才生效**；重启前先杀掉所有 CommandLine 含 `backend/app.py` 的 python 进程
- **检查 Docker 容器 `sister-report` 是否在跑**（它会抢 5000 端口跑旧代码镜像，导致"改代码不生效"）

---

## 二、数据库结构（stats.db）

| 表名 | 说明 |
|------|------|
| `users` | 用户白名单（uid, nickname, role: admin/hall_manager, hall_name 厅名同步） |
| `users_simple`（视图） | 简化三列：UID / 管理的厅名 / 身份（中文） |
| `hall_managers` | 厅运营UID与大厅对应关系（172条记录） |
| `team_detail` | 姐妹团明细数据（按快照日期累积，查询需按 team_id 去重） |
| `weekly_report` | 周报汇总数据（按大厅+周维度，`hall_name='all'` 为全平台汇总） |
| `stats_daily` | 每日统计数据（**只有 `hall_name='全部'` 的全平台数据**） |
| `hall_stats` | 大厅级统计快照 |
| `hall_revenue_daily` | 厅总流水日表（bigdata 大厅主题页，date+hall_id 唯一） |
| `trend_data` | 趋势数据 |
| `alerts` | 预警记录（含 is_resolved 归档标记） |
| `crawl_log` | 抓取日志 |

### 关键数据
- 管理员UID：`34315471`（role=admin）
- 厅运营数量：54 人（role=hall_manager）
- 在营厅约 161 个，有姐妹团业务的约 153 个
- 厅运营平均管理：3-6 个厅

---

## 三、当前正式版结构（2026-09-01 核对）

> 统一落地（阶段1-8）+ 预览对齐（去 emoji + 补齐缺失板块）已全部完成，正式版为 **5 页结构** + 侧栏「姐姐筛选」子标签：

1. **概览页** — 搜索栏（类型/组/大厅/时间三级联动）+ 团分析 + 核心指标 6 KPI + 平台级预警 3 卡 + 毕业妹妹留存跳转 + 明细数据
2. **预警中心** — 平台级预警 4 卡（留存率 / 主动解散占比 / 新成团数 / 解散时间分布）+ 躺平预警（连续未陪档分布 + 大厅分布 + 名单）
3. **厅分析** — 厅排行 + 各厅进行中团占比 + 礼物流水周趋势 + 单厅下钻 + 单厅解散原因构成
4. **姐姐分析** — 姐姐画像 + 候选池（培养力总分）+ 毕业妹妹留存；侧栏「姐姐筛选」子标签直达候选池
5. **UID查询** — 单用户下钻

- 前端 JS 模块（11 个）：config / utils / api / auth / render / app / workbench / insight / warncenter / lyingflat；**workbench/insight/render 仍被 index.html 引用（非死代码）**
- 登录页：粒子交互背景 + 点点开黑 LOGO + Toast 反馈
- Cookie 落地状态灯（绿/红可视化 + 手动刷新）+ 自动保活 + `/api/cookie/auto-login`
- 预览原型 `preview-visual.html` 持续迭代，作为后续改动的施工蓝图

### 历史功能记录（2026-08-12 前的旧版模块，已被 5 页结构取代）
周报概览 / 核心趋势 / 对比分析 / 明细数据 / 政策评估 等独立标签页已合并进新结构。

---

## 四、指标口径定稿汇总（2026-08-31）

> **唯一权威源**：`docs/统一落地草稿.md`（含后端改点 + `app.py` 行号）。本表只作速查，两份不一致以草稿为准。

### 主指标：姐妹关系持续率（替换带团毕业率）
- **姐妹关系持续率 = Σ(已结束团实际存续天数) ÷ (已结束团数 × 30) × 100%**，其中 30 = 毕业线天数（姐妹团满 30 天自动毕业），分母 =「所有已结束团都撑满」的理论满分
- 毕业团按 30 天 clamp（快照滞后会虚高 31/32）；解散团按 `dissolve_date − form_date` 实际天数；**进行中团不进分子分母**
- 数学上 = 毕业率 + Σ(解散团存续比例)/已结束团数，**天然 ≥ 毕业率**（持续率是毕业率的「连续化」升级）
- **带团毕业率**降为**辅助副行**：自然毕业团 ÷ 已结束团（`dissolve_reason='毕业'`）

### 培养力总分
- **v1（当前落地）**：`0.4×姐妹关系持续率 + 0.35×妹妹成长(封顶映射) + 0.25×培养升牌率`
- **v2（占位待定，后面再改）**：`0.4×带团毕业率 + 0.3×妹妹毕业后留存 + 0.2×妹妹成长 + 0.1×姐姐流水牌子`（「毕业后留存」「姐姐流水牌子」两个新因子口径待定义）
- 妹妹成长归一化：`min(成长分/月, 20)/20×100`（**T=20 已定稿 2026-08-31**：定稿口径分母=有成长的团，实测 P95≈22 分/月，头部约 7% 触顶；T=10/30 均已实测否决）；妹妹成长用难度加权分（指数 2^(秩-1)）；培养升牌率 = 妹妹成长覆盖率（有成长团 ÷ 历史团）
- **两个 30 的辨析**：持续率公式的 30 = 毕业线天数（业务规则，不动）；归一化的 T = 满分锚点（统计刻度，可调），二者仅数字巧合

### 预警中心
- 平台级预警第 4 卡 = **解散时间分布**：非毕业解散按「成团→解散」存续天数分 4 周直方图，第 2 周（8-14 天）高峰；下方明细按大厅展示（受影响厅 + 高发周）
- 躺平预警 = 连续 ≥4 天未陪档；自动解散 = 连续 5 日未陪档；活跃率并入躺平模块 KPI
- 解散原因 **8 类**定稿（新增生态违规 / 等级低于铜牌）；官方 6 条解散规则

### 结构 / 其他
- 姐妹团 = 1 姐 1 妹；姐姐可同时多团多妹（最多 2 团 2 妹）；**毕业线 = 30 天（不是牌子）**
- 搜索栏「组」= 父集 /「厅」= 子集（`captain_score_v2.php` 需爬真实映射）
- 候选 / 头牌 / 风险打标阈值 = 持续率 ≥ **65%**（已定稿 2026-08-31，实测全量 45% / 已结束团≥2 口径 38% 达标）
- 主动解散占比 ≠ 解散率；`active_dissolved_count` = 主动解散数

---

## 五、变更记录（2026-08-12 ~ 08-13，更早的见 git log）

### ✅ v1.5.0 姐妹团数据强化（2026-08-13）
- 厅排行榜新增「进行中姐妹团」「姐妹团周流水（礼物奖励口径）」两列，与厅总流水并列
- 核心趋势页并入工作台：删除重复周级图，日级本周vs上周叠加图移入工作台趋势区
- 四象限散点图替换为「厅留存率分布」直方图
- 大厅对比柱状图点击联动：明细表+存活分析按点击厅过滤
- 姐姐排行榜支持 当日/当周/当月 切换（周/月为跨快照去重累计）+ 表头点击排序
- 头牌依赖度过滤 <500 元小厅噪音
- （注：此版基于旧版 8 页结构，后被 4 页统一落地取代）

### ✅ v1.4.2 Cookie 保活 + 端口冲突根因（2026-08-13）
- **端口冲突根因**：Docker 容器 `sister-report`（旧代码镜像）一直映射 0.0.0.0:5000，
  与本地 python 进程同时监听——请求被随机分流，这就是"改了代码没生效"反复出现的真正原因。
  已 `docker stop sister-report`。注意它可能有 restart 策略，Docker Desktop 启动时可能复活
- Cookie 保活：`crawler/cookie_keepalive.py`，Flask 后台线程每 30 分钟 + 每日抓取后各跑一次；
  状态写 data/keepalive_status.json（不入库 git），面板展示
- 两个系统登录页都有图形验证码（bigdata rand_code / dedecms validate），
  完全自动重登需要 OCR，暂用保活方案（后续已加 `/api/cookie/auto-login`）

### ✅ v1.4.1 项目清理与文档整理（2026-08-13）
- 删除：debug_uid_*.html、fix_*.py、check_*.py 等一次性脚本、flask.log、*.bak、重复虚拟环境 .venv-1
- tests/ 只保留 3 个实用脚本（verify_data_accuracy / backfill_hall_revenue / make_logo）
- 历史文档（PRD/需求文档/进度清单等）移入 docs/；LOGO 源图移入 frontend/assets/

### ✅ v1.3.3 厅总流水接入
- **厅总流水数据源**：bigdata「大厅主题」`/roomnew/indextest/4`（最近完整一天的全厅日流水，含厅ID），
  单厅历史 `/roomnew/index/4/1/all/all/{hall_id}`（近29天日流水）
- 新表 `hall_revenue_daily(date, hall_id, hall_name, hall_revenue)`，UNIQUE(date,hall_id)，写入幂等
- 已验证：特别的人 08-03~08-09 = 338,955.50 与用户口径完全一致
- 口径备忘：weekly_report.total_reward=姐妹团礼物奖励；hall_revenue_daily=厅总流水（差异约 3 倍）

### ✅ v1.12.0 离职原因细分（2026-09-03）
- `crawler/leaver_refine.py`：「姐妹任意一方从大厅离职自动解散」→ 姐姐/妹妹/双方
  - `NNNNNN 离职` 格式（bigdata 直接给 UID）：按 UID 匹配团成员，零请求
  - 通用文案：UID 查询双方「最近一次离职时间」vs dissolve_date（精确日优先，±1 天容忍）
  - 结果存 `team_leaver(team_id PK, leaver_role, leaver_uid, leave_time, source, checked_at)`，
    回写 team_detail.dissolve_reason；**每日抓取后必须 apply_leaver_reasons() 重写**（新快照带回原始文案）
  - 已接入 daily_crawl.py（抓取后、预警前）；查询失败不写记录下轮重试，未判定不重试
- **server1 Cookie TTL 极短**（几分钟级）：refine 遇 CookieExpiredError 自动 `refresh_uid_cookie()` 续跑（上限 3 次）
- DISSOLVE_REASON_CASE 拆出「姐姐离职/妹妹离职/双方同日离职」，明细表原因筛选同步加选项
- 实测：56181 解散日=妹妹 last_leave_time ✓；首批 5 团全部分出（3 妹妹 2 姐姐）

### ✅ v1.11.0 明细表生命周期方框（2026-09-03）
- 明细表「已成团天数」= 1×4 方框条（30 天 4 周）+ 天数；点击方框弹层看该周明细
- 新接口 `GET /api/team/<id>/life-weeks`：按 form_date 划「成团第 N 周」（第 4 周 9 天）；
  妹妹流水/任务=快照增量，奖励=窗口求和，姐姐流水=team_sister_revenue 自然周按重叠天数折算（约值）
- **快照噪声坑**：团解散后仍出现在每日快照里且累计流水偶有增长 → 「未经历」周（gone）指标必须置空
- **快照缺口坑**：个别团某些天掉出快照（每天仅 1000 行）→ 该周可能无数据（显示 —），
  且下一周增量会跨缺口累计（轻微信度损失，可接受）
- form_date 老格式（'07-15星期三'）→ 用最早快照 days_since_formed−1 反推成团日
- **周增量口径**：只累加窗口内相邻快照差 + 窗口首日增量（需前一天有快照）；缺口周显示 —，
  跨缺口边界增量标 ≈（partial）。曾用「期末−期前最后快照」会把缺口前几周并入，已修复（09-03）
- 弹层字段：周流水（姐折算≈/妹精确）、累计流水（截至该周末）、任务活跃、双方牌子；无礼物奖励
- 团详情弹窗（renderTeamDetailModal）：顶部卡片=妹妹/姐姐累计流水（无奖励金额）；
  周表数据源=life-weeks（成团第N周），`/api/team/<id>/weekly`（自然周）已不被弹窗使用但保留；
  openTeamDetail(idx, focusWeek) 第二参数=方框进入时聚焦的周

### ✅ v1.10.0 概览页按月统计（2026-09-02）
- 筛选栏下移到「毕业妹妹留存」与「核心指标」之间；时间拆为「月+周」双下拉（先选月，周下拉=整月+与该月有交集的周，见 `fillWeekSelectForMonth`）
- **月视图口径（重要）**：姐妹团 30 天生命周期 → 跨整月的 (期末−新成团)/期初 留存口径必然趋 0
  （8 月实测：08-01 在榜 591 团到 08-31 全部毕业/解散，交集幸存者=0），无业务意义。
  故 `/api/kpi` 月范围：率类=月内各周周均值（周归属按周中点周四判定，见 `apply_month_avg_rates`），
  计数类=整月合计；本月·至今时计数对比上一月同期窗口（MTD vs MTD），率类对比上一月整月周均值
- **form_date 混合格式坑**：7 月下旬前快照的 form_date 是 `06-29星期一` 中文格式（无年份），
  之后才是 ISO `2026-08-01`。做日期范围比较时对老数据不生效；`date(form_date)` 对中文格式返回 NULL
- KPI 卡「较上周/较上月」随 `period_type` 切换

### ✅ v1.3.2 数据口径修正（重要）
- **reward_amount 口径**：team_detail.reward_amount = 快照当日发放的礼物奖励（非累计！）；
  weekly_report.total_reward 才是周累计；个人累计总流水只在 server1 UID 查询系统
- **快照重复行**：一天多次抓取会追加重复行。已在 save_detail 写入前 DELETE 当天快照（幂等），
  所有 team_detail 查询按 team_id 去重（rowid IN (SELECT MAX(rowid) ... GROUP BY team_id)）
- 数据是 T+1（每日抓取），非实时；页面有新鲜度色点

### ✅ v1.3.1 修复与验证
- 「团长分析」更名「姐姐分析」；UID Cookie 热加载（页面更新立即生效）
- 新增 `tests/verify_data_accuracy.py`：API vs SQL 独立重算 46 项全部 PASS
- 注意：Git Bash 的 /tmp 与 Windows python 的 /tmp 不一致，测试脚本输出用相对路径

### ✅ 更早的修复（第一期~第三期工作台改版、三期功能）
- 厅运营权限过滤、切换大厅不刷新（图表实例未 dispose）、UID 查询 GBK 编码崩溃、
  工作台卡墙/排行榜/预警横幅、政策评估/姐姐分析/预警中心页面、users_simple 视图、
  侧栏布局与折叠、登录页粒子背景与 LOGO 等——详见 git log（d550cc8 ~ 22e0fe4）

---

## 六、当前存在的问题（2026-09-01 核对）

### ⚠️ 待处理
1. ~~占位值重测~~ **已完成（2026-08-31）**：持续率阈值 65% 定稿（全量45%/已结束团≥2口径38%达标）；成长封顶 T=20 定稿（P95≈22，头部7%触顶）；分析脚本 `tests/analyze_thresholds.py`
2. **培养力总分 v2 两个新因子口径未定义** — 妹妹毕业后留存 / 姐姐流水牌子（v1 四因子已落地，v2 只是方向占位）
3. **毕业妹妹留存的「妹妹留存率 / 毕业后 30 天留存 / 带妹代数 / 来源」仍为「待落地」占位** — 依赖「回访」数据，后端暂无此口径（`recent_graduates` 表已接真数据）
4. ~~根目录实验残留文件~~ **已清理（2026-09-01）**：cj.txt / jqmobo2.js / p.html / q.html / wjx.html / tests/v2_analysis.txt 已删除
5. **Docker 容器 sister-report 可能复活** — Docker Desktop 启动时注意检查 5000 端口归属；重启后端前先杀掉所有 CommandLine 含 `backend/app.py` 的 python 进程（Windows 下 SO_REUSEADDR 允许多进程同端口，09-01 又复发过一次）

### ⚠️ 待优化
6. **前端框架** — 原生 HTML/JS，迁移 Vue/React 未实施
7. **数据准确性持续核对** — `tests/verify_data_accuracy.py` 可当回归脚本用（注意其校验的是旧版接口）
8. ~~frontend/js 旧模块 workbench/insight/render 是否仍被引用~~ **已确认（2026-09-01）**：三者仍被 index.html 引用，非死代码，无需清理

---

## 七、统一落地清单（执行情况）

> 权威版见 `docs/统一落地草稿.md`。**统一落地已分批执行（08-18 ~ 08-25）**，主要项已完成：
> 4 页结构、warncenter 接口与预警卡、持续率主指标、解散原因 8 类、Cookie 状态灯等均已提交。

**剩余待定 / 占位（收口清单）**
- ~~持续率阈值 65%、妹妹成长 T=30 —— 需真实数据重测~~ **已收口 2026-08-31：65% / T=20 定稿**
- 培养力总分 v2 四因子（两个新因子口径未定义）
- ~~预览原型中 08-25 之后的改动（解散时间分布分周直方图入预警网格、时间选择器、全量弹窗等）~~ **已落地正式版（统一落地 8 阶段全量完成）**

---

## 八、关键文件说明

```
D:\姐妹团看板系统
├── preview-visual.html     # 预览原型（持续迭代，最新08-29，施工蓝图）
├── backend/app.py          # Flask主入口（所有API路由）
├── frontend/
│   ├── index.html          # 主页面（4页结构：概览页/厅分析/姐姐分析/UID查询）
│   ├── login.html          # 登录页（粒子交互背景 + 点点开黑LOGO + Toast反馈）
│   ├── assets/             # logo-ddkh.png 等静态资源（含LOGO源图）
│   ├── css/style.css       # 样式（设计令牌 --wb-*，后者覆盖前者）
│   └── js/                 # config/api/app/auth/utils + workbench/insight/render（部分旧模块待确认是否仍被引用）
├── crawler/
│   ├── crawler.py          # bigdata抓取（统计/明细/趋势/大厅日流水）
│   ├── uid_crawler.py      # UID查询（Cookie热加载 cookie.json）
│   ├── cookie_keepalive.py # Cookie 保活（30分钟周期）
│   ├── hall_manager_crawler.py  # 厅运营UID抓取
│   └── db.py / metrics.py / alerts.py / alerts_engine.py
├── data/stats.db           # 唯一使用中的数据库（其余 .db 是历史遗留）
├── docs/                   # PRD及历史文档 + 统一落地草稿.md(权威口径/落地清单) + adr/ + archive/
├── tests/                  # 只保留3个实用脚本（verify/backfill/make_logo）
└── daily_crawl.py          # 定时抓取入口（含预警检测 + Cookie保活）
```

---

## 九、近期Git提交记录

```
a63b6e1 概览页筛选栏下移 + 时间新增按月统计（月视图率类=周均值/计数=整月合计/对比上一月）
8e3d48f 修复图空白/不显示: 解散原因500 + 卡片拉伸留白 + 折线悬浮留白
f36e45e 对齐预览版: 搜索栏单栏 + 姐姐总览合并删排行 + 毕业妹妹留存近似落地 + 预警图去留白
63d65ec 搜索栏对齐预览版: 补 类型/组/大厅 三级联动 + 后端 /api/hall-groups
10e3a27 团队明细弹窗对齐预览版: 补「按周流水与牌子走势」表 + 后端 /api/team/<id>/weekly
76695c1 对齐预览版: 解散原因环形图加中心总数+外标签 + 预警中心补预警规则表 + 姐姐分析补模块标题
0bc619b 对齐预览版: 6张KPI卡(毕业妹妹数/妹妹留存率) + 姐姐排行/躺平留白修复 + 明细搜索占位与列顺序
5af3705 修复概览页9项问题: 顶部搜索栏+团ID搜索+多妹合并大行+全宽布局+预警中心数据+厅分析解散构成两图+该厅团明细+毕业妹妹+象限图上移+弹窗改TOAST
4cf01fd 删自选对比旧模块 + 收尾去 emoji
cb819d4 姐姐分析：候选池改侧栏子标签 + 毕业妹妹留存 module
76ac2a4 厅分析补多图 + 单厅解散原因构成
bde1b28 预警中心补躺平预警（妹妹维度）+ 接口补妹妹字段
e3f03c7 概览页对齐预览版顺序 + 补「毕业妹妹留存」跳转块
956c404 对齐预览版侧栏：去图标字形 + 副标题对齐 + 新增姐姐筛选子标签
4400b97 对齐预览版：删除全部装饰 emoji（保留 ▾▼▲✕«☰↑ 与状态符 ✓✗）
875ca49 预览对齐: 补齐5页「数据说明」口径块(7处) + 概览页预警卡(跳预警中心)
（更早：统一落地批次 1c42ee2~db1285e (阶段1-8)；口径定稿 bda5ee2/ceb6401；工作台三期改版 d550cc8~f127460 (08-12~08-13)）
```

---

## 十、接口清单（2026-08-31 核对）

| 接口 | 说明 |
|------|------|
| `GET /api/check-auth` / `POST /api/login` / `POST /api/logout` / `GET /api/captcha` | 认证 |
| `GET /api/halls` / `GET /api/hall-overview` / `GET /api/hall-stats` / `GET /api/hall-groups` | 大厅（按角色过滤）+ 组→大厅层级（搜索栏三级联动） |
| `GET /api/kpi` / `GET /api/weekly-report` / `GET /api/weekly-team-detail` | 周报与KPI（KPI 支持月范围 `2026-08-01|2026-08-31`，返回 `period_type`，月视图率类=月内周均值、计数=整月合计、对比上一月） |
| `GET /api/daily-retention` / `GET /api/daily-events` / `GET /api/trends` / `GET /api/trend-insights` | 趋势 |
| `GET /api/survival` / `GET /api/dissolve-reasons` | 存活与解散原因 |
| `GET /api/warncenter` / `GET /api/alerts` / `GET /api/alerts-center` / `POST /api/alerts/<id>/resolve` | 预警 |
| `GET /api/lying-flat` / `GET /api/lying-detail` | 躺平分析 |
| `GET /api/sister-profile` / `GET /api/sister-detail` / `GET /api/sister2-profile` / `GET /api/sister2-detail` | 姐姐/妹妹画像（sister2 用于毕业妹妹晋升查询） |
| `GET /api/grad-retention` | 毕业妹妹留存（回访/近似双口径） |
| `GET /api/team/<id>/weekly` | 团队明细弹窗「按周流水与牌子走势」 |
| `GET /api/talent-pool` / `GET /api/talent-actions` / `POST /api/talent-actions/<id>/result` | 候选池与培养动作 |
| `GET /api/captains` / `GET /api/policy-impact` / `GET /api/policy-attribution` | 姐姐排行 / 政策评估 |
| `POST /api/uid-query` / `GET /api/uid-query/types` / `GET /api/search-suggest` | UID查询 |
| `GET/POST /api/cookie` / `/api/cookie/bigdata` / `POST /api/cookie/auto-login` / `GET /api/keepalive-status` | Cookie 管理 |
| `GET /api/export/weekly` / `detail` / `pdf-report` | 导出 |
| `GET /api/detail-table` / `GET /api/last-update` | 明细表 / 更新时间 |

---

## 十一、Cookie来源说明

- **UID查询Cookie** → `server1.tuwan.com:10010`（队长数据查询）✅ 已恢复有效（08-13 更新）+ 保活运行中
- **数据抓取Cookie** → `bigdata.tuwan.com`（姐妹团统计数据）✅ 保活运行中
- **厅运营Cookie** → `http://server1.tuwan.com:10010/fly2013/play_chatroom.php`（厅运营UID抓取）
- 保活机制：30 分钟周期自动访问保活；失效时面板亮红灯，手动更新后自动恢复

---

## 十二、下次会话建议

1. ~~重测占位值~~ 已完成（65% / T=20 定稿，2026-08-31）
2. 定培养力总分 v2 两个新因子口径（毕业后留存 / 姐姐流水牌子）——注意：毕业后留存按「再组团」口径实测为 0%（毕业即离团），真口径需 server1 批量查毕业妹妹近况
3. ~~preview-visual.html 08-25 后预览改动落地正式版~~ **已完成（2026-09-01）**：5 页结构 + 去 emoji + 补齐缺失板块
4. ~~清理根目录实验残留文件~~ **已完成（2026-09-01）**
5. 定毕业妹妹留存「妹妹留存率 / 毕业后 30 天留存 / 带妹代数 / 来源」真实口径（依赖「回访」数据，当前占位）
6. 后端重启注意：先杀所有 `backend/app.py` python 进程 + 确认 Docker `sister-report` 容器没在跑
