# 姐妹团看板系统 — 项目上下文记忆文档

> 生成时间：2026-08-12 · 最近更新：2026-08-31
> 项目路径：`D:\姐妹团看板系统`
> Git最新提交：`见「九、近期Git提交记录」`

---

## 一、项目概述

**姐妹团数据统计报表** — 面向「点点开黑」平台运营团队的Web数据看板，追踪姐妹团（姐姐+妹妹配对）的成团、留存、解散、流水等核心指标，支持政策上线效果追踪。

### 技术栈
- **后端**：Python 3 + Flask + SQLite
- **前端**：原生 HTML/JS + ECharts（本地文件）
- **数据抓取**：Python爬虫（BeautifulSoup + Requests）
- **部署**：本地运行 / Docker
- **认证**：Cookie-based（auth_uid）+ 验证码登录

### 运行方式（重要）
- 必须用项目虚拟环境启动：`.venv/Scripts/python.exe backend/app.py`
- ❌ 不要用 kimi-desktop 自带的 python（缺 flask 模块）
- 日志输出到 `logs/flask.log`
- **代码修改后必须重启后端才生效**（已踩过坑：改了代码没重启，排查半天发现跑的是旧进程）

---

## 二、数据库结构（stats.db）

| 表名 | 说明 |
|------|------|
| `users` | 用户白名单（uid, nickname, role: admin/hall_manager） |
| `hall_managers` | 厅运营UID与大厅对应关系（172条记录） |
| `team_detail` | 姐妹团明细数据（最新快照） |
| `weekly_report` | 周报汇总数据（按大厅+周维度，`hall_name='all'` 为全平台汇总，其余为单厅） |
| `stats_daily` | 每日统计数据（**只有 `hall_name='全部'` 的全平台数据**，无单厅维度） |
| `hall_stats` | 大厅级统计快照 |
| `trend_data` | 趋势数据 |
| `alerts` | 预警记录 |
| `crawl_log` | 抓取日志 |

### 关键数据
- 管理员UID：`34315471`（role=admin）
- 厅运营数量：约40+人（role=hall_manager）
- 大厅总数：174个（weekly_report 中有单厅数据的约150个）
- 厅运营平均管理：3-5个厅（如 31053950 管6个厅）

---

## 三、已完成的功能

### 核心模块
1. **📊 周报概览** — KPI卡片（新成团、进行中、留存率、解散率、礼物奖励金额、主动解散占比）
2. **📈 核心趋势** — 留存率/解散率/礼物流水/任务活跃度 四张趋势图
3. **🔍 对比分析** — 本周vs上周指标对比表 + 各大厅数据对比柱状图 + 新成团vs解散双轴图
4. **📋 明细数据** — 姐妹团明细表格，支持搜索/排序/分页/状态筛选
5. **👤 UID查询** — 查询队长UID数据，含本周vs上周对比、姐妹团参与明细、姐姐vs妹妹对比

### 登录与权限
6. **🔐 登录页面** — 深色渐变背景 + 粒子动效 + iOS玻璃卡片 + 字母验证码
7. **权限系统** — 白名单制（admin / hall_manager 两种角色）
8. **Cookie管理** — 双Cookie分别管理（UID查询Cookie + 数据抓取Cookie）

### 厅运营视角（已完成并验证 ✅）
9. **大厅下拉框** — 厅运营登录后只显示自己管理的厅 + 「所有大厅」选项
10. **对比分析过滤** — 厅运营默认只看自己管理的厅；选择「所有大厅」可看全平台
11. **数据隔离** — `/api/halls` 和 `/api/hall-stats` 均按角色过滤
12. **KPI按厅过滤** — `/api/kpi` 支持 hall 参数，单厅查询 weekly_report 对应厅数据；成就达成率仅全平台有数据（单厅返回0）

### 数据导出
13. **PDF导出** — 周报概览+核心趋势+预警（reportlab）
14. **CSV导出** — 周报数据、明细数据、UID查询结果

### 自动化
15. **定时任务** — `daily_crawl.py` 每日自动抓取数据（需有效Cookie）
16. **数据更新** — 自动写入 stats.db 并更新 last_update.json

### 预览原型（先预览后定稿，2026-08 新增）
17. **5 页完整预览** — `preview-visual.html`（throwaway 原型，非正式源码）：概览页 / 预警中心 / 厅分析 / 姐姐分析 / UID查询，手绘 SVG 图表（非 ECharts），作为统一落地的施工蓝图
18. **工作流** — 先预览 → 逐页定稿 → 一次性统一改写正式源码；`backend/app.py` + `frontend/*` 在定稿前不动

---

## 四、指标口径定稿汇总（2026-08-31）

> **唯一权威源**：`docs/统一落地草稿.md`（含后端改点 + `app.py` 行号）。本表只作速查，两份不一致以草稿为准。

### 主指标：姐妹关系持续率（替换带团毕业率）
- **姐妹关系持续率 = Σ(已结束团实际存续天数) ÷ (已结束团数 × 30) × 100%**，其中 30 = 毕业线天数（姐妹团满 30 天自动毕业），分母 =「所有已结束团都撑满」的理论满分
- 毕业团按 30 天 clamp（快照滞后会虚高 31/32）；解散团按 `dissolve_date − form_date` 实际天数；**进行中团不进分子分母**
- 数学上 = 毕业率 + Σ(解散团存续比例)/已结束团数，**天然 ≥ 毕业率**（持续率是毕业率的「连续化」升级）
- **带团毕业率**降为**辅助副行**：自然毕业团 ÷ 已结束团（`dissolve_reason='毕业'`）

### 培养力总分
- **v1（当前落地）**：`0.4×姐妹关系持续率 + 0.35×妹妹成长(封顶映射) + 0.25×培养升牌率`
- **v2（占位待定，后面再改）**：`0.4×带团毕业率 + 0.3×妹妹毕业后留存 + 0.2×妹妹成长 + 0.1×姐姐流水牌子`（「毕业后留存」「姐姐流水牌子」两个新因子口径待定义）
- 妹妹成长归一化：`min(成长分/月, 30)/30×100`（T=30 待重测 95 分位）；培养升牌率 = 妹妹成长覆盖率（有成长团 ÷ 历史团，去掉「团存活」条件，否则分子封顶 2）

### 预警中心
- 平台级预警第 4 卡 = **解散时间分布**：非毕业解散按「成团→解散」存续天数分 4 周直方图，第 2 周（8-14 天）高峰；下方明细按大厅展示（受影响厅 + 高发周）
- 躺平预警 = 连续 ≥4 天未陪档；自动解散 = 连续 5 日未陪档；活跃率并入躺平模块 KPI
- 解散原因 **8 类**定稿（新增生态违规 / 等级低于铜牌）；官方 6 条解散规则

### 结构 / 其他
- 姐妹团 = 1 姐 1 妹；姐姐可同时多团多妹（最多 2 团 2 妹）；**毕业线 = 30 天（不是牌子）**
- 搜索栏「组」= 父集 /「厅」= 子集（`captain_score_v2.php` 需爬真实映射）
- 候选 / 头牌 / 风险打标阈值 = 持续率 ≥ **65%**（占位，落地前按真实分布重测）
- 主动解散占比 ≠ 解散率；`active_dissolved_count` = 主动解散数

---

## 五、本次会话修复记录（2026-08-12）

### ✅ v1.4.2 Cookie 保活 + 端口冲突根因（2026-08-13）
- **端口冲突根因**：Docker 容器 `sister-report`（docker-compose，旧代码镜像）一直映射 0.0.0.0:5000，
  与本地 python 进程同时监听——请求被随机分流到两个服务端，这就是"改了代码没生效"反复出现的真正原因。
  已 `docker stop sister-report`。注意它可能有 restart 策略，Docker Desktop 启动时可能复活。
- Cookie 保活：`crawler/cookie_keepalive.py`，Flask 后台线程每 30 分钟跑一次 + 每日抓取后跑一次；
  状态写 data/keepalive_status.json，面板展示
- 两个系统登录页都有图形验证码（bigdata rand_code / dedecms validate），
  完全自动重登需要 OCR（ddddocr），暂用保活方案

### ✅ v1.4.1 项目清理与文档整理（2026-08-13）
- 删除：debug_uid_*.html、fix_*.py、check_*.py 等一次性脚本、flask.log、*.bak、重复虚拟环境 .venv-1
- tests/ 只保留 3 个实用脚本（verify_data_accuracy / backfill_hall_revenue / make_logo）
- 历史文档（PRD/需求文档/进度清单等）移入 docs/；LOGO 源图移入 frontend/assets/
- README 功能表/技术栈/项目结构全面更新
- 注意：uid_crawler 每次查询仍会生成 debug_uid_*.html 调试文件，需定期清理（或改代码关闭）

### ✅ v1.3.3 厅总流水接入
- **厅总流水数据源**：bigdata「大厅主题」`/roomnew/indextest/4`（最近完整一天的全厅日流水，含厅ID），
  单厅历史 `/roomnew/index/4/1/all/all/{hall_id}`（近29天日流水）
- 新表 `hall_revenue_daily(date, hall_id, hall_name, hall_revenue)`，UNIQUE(date,hall_id)，写入幂等
- 排行榜/四象限的周流水改用 SUM(hall_revenue) 按周区间聚合（/api/hall-overview 合并输出）
- 已验证：特别的人 08-03~08-09 = 338,955.50 与用户口径完全一致
- 回填脚本 `tests/backfill_hall_revenue.py`（一次性，约 3-5 分钟）
- 口径备忘：weekly_report.total_reward=姐妹团礼物奖励；hall_revenue_daily=厅总流水；
  两者差异约 3 倍（特别的人 11.6w vs 33.9w/周）

### ✅ v1.3.2 数据口径修正（重要）
- **reward_amount 口径**：team_detail.reward_amount = 快照当日发放的礼物奖励（非累计！），
  SUM 全平台约 1.4k~5.4k/天；weekly_report.total_reward 才是周累计（~48k/周）；
  真正的个人累计总流水只在 server1 UID 查询系统（妹妹累计流水字段 sister_revenue 也是累计口径，~3.8万）
- **快照重复行**：一天多次抓取会向 team_detail 追加重复行（08-11 有 2000 行/1007 团）。
  已在 save_detail 写入前 DELETE 当天快照（幂等），并在所有 team_detail 查询按 team_id 去重
  （rowid IN (SELECT MAX(rowid) ... GROUP BY team_id)）
- 姐姐分析页「累计流水」→「当日奖励」，头牌依赖度同步改为当日口径，标注快照日期
- 数据是 T+1（每日抓取），非实时；页面有新鲜度色点

### ✅ v1.3.1 修复与验证
- 「团长分析」更名「姐姐分析」（标签/标题/表头）
- UID Cookie 热加载：`_build_headers()` 每次实例化重读 cookie.json，页面更新立即生效
  （注意：当前保存的 Cookie 服务端确实判定未登录——需重新登录 server1 后台后立即复制完整 Cookie）
- 新增 `tests/verify_data_accuracy.py`：API vs SQL 独立重算 46 项，全部 PASS
- 注意：Git Bash 的 /tmp 与 Windows python 的 /tmp 不一致，测试脚本输出文件用相对路径

### ✅ 第三期「运营闭环」已落地（v1.3.0）
- 政策评估页：前4周vs后4周均值对比卡 + 分厅响应度发散条形图 + 明细表（/api/policy-impact）
- 团长分析页：团长排行 + 头牌依赖度警示（/api/captains，team_detail 按 sister_uid 聚合）
- 预警中心页：历史预警 + 已处理归档（/api/alerts-center + POST /api/alerts/<id>/resolve，复用 is_resolved 字段）
- 新前端文件 js/insight.js；标签页增至 8 个
- 坑：index.html 的 tab-uid 面板原本就少一个 `</div>`，新面板插进去会被嵌套进 uid 面板导致空白，
  插入新面板前必须先补全闭合（已修）

### ✅ 第二期「管理决策增强」已落地（v1.2.0）
- 对比分析页：厅四象限散点图（流水×留存，中位数分象限，点击切厅）
- 工作台：选中具体厅时，留存/解散趋势叠加平台均值虚线（wbPlatformWeekly 缓存）
- 核心趋势页：日级本周vs上周叠加图（新接口 /api/daily-events，team_detail 快照派生）
- 明细页：存活分析卡片（新接口 /api/survival：天数分布+7/14/30日存活率+政策前后对比）
- 注意：重启后端时曾出现多个 python 进程同时占用 5000 端口（Windows SO_REUSEADDR），
  导致新旧代码随机响应。重启前必须杀掉所有 CommandLine 含 backend/app.py 的 python 进程。

### ✅ 第一期「工作台」改版已落地（v1.1.0）
- 首页概览页 → 工作台：厅体检卡墙（红黄绿+sparkline）、预警横幅（真实原因、可点击联动）、
  KPI 3大卡+3小卡、4 趋势图；管理员视角为厅排行榜（含位次变化）
- 新增后端 `/api/hall-overview`（按角色返回厅列表+近7周数据）
- `api_kpi` 支持 hall 过滤；所选周无数据自动回退最新一周（含 'all'）
- 前端新增 `js/workbench.js`；`refreshData/onWeekChange/switchTab` 已改接工作台
- 记住大厅/周选择（localStorage wb_hall/wb_week）；数据新鲜度色点
- 工具栏移除导出CSV/PDF按钮（接口保留）；视觉令牌化（style.css 末尾 --wb-* 变量）
- 已用 Playwright 验证厅运营/管理员/手机三视角真实数据渲染正常

### ✅ 已修复
1. **厅运营看到全部大厅** — 根因：跑的是旧进程（昨晚启动，代码今天才改）。重启后端后生效。
   排查时还发现并清理了多个残留 python 进程互相抢端口的问题。
2. **切换大厅后数据不更新** — 两处根因：
   - 前端：`refreshData()` 调用 `initTrendChart()` 等函数时未先 dispose 旧图表实例，
     函数内部 `if (charts.xxx) { resize(); return; }` 直接返回，永远不发新请求。
     修复：`frontend/js/app.js` 的 `refreshData()` 先 dispose 再 init（与 `onWeekChange` 同款模式）。
   - 后端：`/api/kpi` 接收 hall 参数但查询写死 `hall_name='all'`，KPI卡片永远不随厅变化。
     修复：`backend/app.py` 的 `api_kpi` 按 hall 参数过滤 weekly_report。
3. **UID查询报 GBK 编码错误** — `'gbk' codec can't encode character '\u274d'`：
   Windows 控制台是 GBK，`crawler/uid_crawler.py` 打印含 ❍ 等符号的昵称时抛
   UnicodeEncodeError 导致接口 500。修复：`backend/app.py` 启动处对
   `sys.stdout/stderr` 做 `reconfigure(errors='replace')`，一劳永逸覆盖所有 print。

### ⚠️ 发现的新问题
4. **UID查询 Cookie 已过期** — 实测 `/api/uid-query` 返回 503「Cookie已过期」，
   需更新 UID 查询 Cookie（页面上有更新入口，或改 `crawler/uid_crawler.py` 顶部 UID_COOKIE_STR）。

---

## 六、当前存在的问题

### ⚠️ 已知Bug / 待处理
1. **UID查询Cookie过期** — 见上，需用户更新 Cookie 后验证真实查询
2. **解散率始终为0** — 修复清单第2项，待实现：`api_kpi` 动态计算解散率
3. **明细数据权限** — 厅运营选择「所有大厅」时，明细数据是否仍受权限限制需确认
4. **数据准确性** — 累计流水 vs 当周礼物流水的数据来源需持续核对
5. **留存率公式** — 是否超过100%的边界情况需监控

### ⚠️ 待优化
6. **前端框架** — 目前为原生HTML/JS，用户曾询问是否需要迁移到Vue/React，暂未实施
7. **测试文件清理** — `debug_uid_*.html` 等测试文件需统一管理和一键清理
8. **Docker启动** — 有时启动超时，需检查容器配置
9. **Cookie过期** — 定时任务因Cookie失效而失败，需定期更新
10. **重复死代码** — `backend/app.py` 第525行附近有一个重复的 `api_hall_stats` 定义（无路由装饰器，不生效），可择机清理

---

## 七、统一落地清单（改源码施工蓝图）

> 权威版见 `docs/统一落地草稿.md`，此处为速查摘要。前置：5 页预览全部完成，待逐页定稿 + 占位值重测后一次性落地。

**后端（`backend/app.py`）**
- `DISSOLVE_REASON_CASE` 补「毕业 / 不在同厅」分支；`ACTIVE_DISS_REASON_SQL` 补「注销」
- `/api/hall-stats` 路由去重（1908 vs 1954 重复定义，后者覆盖前者）
- `/api/lying-flat` 改「连续 ≥4 天未陪档」口径
- 新增 `/api/warncenter`（平台级预警 4 卡 + 解散时间分布分 4 桶）
- `api_sister_profile.retention` / `api_talent_pool.retention` 改「姐妹关系持续率」；候选阈值 ≥65；培养力总分 v1
- 妹妹成长分母 `g['teams']` → `g['grew']`；培养升牌率删「团存活」`alive` 判定；候选牌子门槛 `level_rank >= 2` → `>= 4`（银牌及以上）
- 清理死字段 `dependency`/`survival_rate` 与死接口 `sister2-profile`/`sister2-detail`/`/api/uid-query/types`

**前端（`frontend/*`）**
- 侧栏加「预警中心」tab + 新建 `warncenter.js`；概览页加「平台级预警 4 卡」区块
- 解散原因饼图配色补「毕业」、`detail-reason` 下拉同步
- UID 页 `team_info` 标「妹妹累计流水」口径、导出字段修正
- 候选池表加「培养力总分」列；培养力象限图改「只显示候选 + 密集聚合 + 按厅筛选」；表头排序箭头

**待定 / 占位（落地前需收口）**
- 持续率阈值 65%、妹妹成长 T=30 —— 需真实数据重测
- 培养力总分 v2 四因子（两个新因子口径未定义）

---

## 八、关键文件说明

```
D:\姐妹团看板系统
├── preview-visual.html     # 5页预览原型（throwaway，先预览后定稿，非正式源码）
├── backend/app.py          # Flask主入口（所有API路由）
├── frontend/
│   ├── index.html          # 主页面（侧边栏布局，8个标签页）
│   ├── login.html          # 登录页（粒子交互背景 + 点点开黑LOGO）
│   ├── assets/             # logo-ddkh.png 等静态资源（含LOGO源图）
│   ├── css/style.css       # 样式（设计令牌 --wb-* 在文件中后部，后者覆盖前者）
│   └── js/
│       ├── config.js       # 全局配置/状态
│       ├── api.js          # API封装（loadHalls/loadWeeks/loadDetailTable等）
│       ├── app.js          # 交互（switchTab/refreshData/toggleSidebar等）
│       ├── render.js       # 核心趋势/对比分析页图表
│       ├── workbench.js    # 工作台全部逻辑（卡墙/排行/KPI/趋势/四象限/日级叠加/存活）
│       ├── insight.js      # 政策评估/姐姐分析/预警中心
│       ├── utils.js        # 工具函数（getHallParam/getWeekParam/setChange）
│       └── auth.js         # 登录检查 + 侧栏用户信息渲染
├── crawler/
│   ├── crawler.py          # bigdata抓取（统计/明细/趋势/大厅日流水）
│   ├── uid_crawler.py      # UID查询（Cookie热加载 cookie.json）
│   ├── hall_manager_crawler.py  # 厅运营UID抓取
│   └── db.py / metrics.py / alerts.py
├── data/stats.db           # 唯一使用中的数据库（其余 .db 是历史遗留）
├── docs/                   # PRD及历史文档 + 统一落地草稿.md(权威口径/落地清单)
├── tests/                  # 只保留3个实用脚本（verify/backfill/make_logo）
└── daily_crawl.py          # 定时抓取入口
```

---

## 九、近期Git提交记录

```
ceb6401 口径整理: 姐妹关系持续率定稿(替换带团毕业率作主指标) + 解散时间分布 + 培养力总分0.4×持续率
bb47953 预览: 「破裂时间分布」更名「解散时间分布」，下方明细改按大厅展示(受影响厅+高发周)
39884ee 预览: 破裂时间分布卡补「各周破裂明细」表(4周数+占比)+下钻全部按钮，填掉网格拉伸空段
9a4a3d0 预览: 破裂时间分布分周直方图移入平台级预警网格(顶掉原活跃率卡的空位)
1017cc9 预览: 预警中心「陪档活跃率预警」并入躺平模块(活跃率已在躺平KPI)、改由「破裂时间分布」顶上
e22a64d 预览: 姐姐分析「带团毕业率」改「姐妹关系持续率」 + 预警中心新增破裂时间分布分周直方图
098c7d2 文档: 搜索框分层口径 + 预警中心时间选择器 + 受影响厅全量弹窗 写入README/落地草稿
cc05b3b 预览: 预警中心顶部搜索栏新增时间选择器(本周/上周/自定义周)
94629c1 预览: 预警中心4卡 + 躺平大厅分布 增「查看全部受影响厅」全量弹窗
dd861a7 预览: 预警中心「异常解散」改异常解散总数时序图 + 下方厅按4类集合统计
fab2e36 预览: 预警中心「异常解散」改为4类非毕业柱状图(主动解散/资格失效/违规/不活跃)
be0dcc7 预览: 预警中心「主动解散占比」卡改为「解散原因构成」8类环形图 + 下钻联动
（更早提交见 git log，最初 d550cc8）
```

---

## 十、接口清单

| 接口 | 说明 | 权限 |
|------|------|------|
| `GET /api/check-auth` | 检查登录状态 | 公开 |
| `POST /api/login` | 登录（UID+验证码） | 公开 |
| `GET /api/captcha` | 获取验证码图片 | 公开 |
| `GET /api/halls` | 大厅列表（按角色过滤） | 需登录 |
| `GET /api/kpi` | KPI概览（支持hall参数按厅过滤） | 需登录 |
| `GET /api/trends` | 趋势数据（仅全平台，前端未使用） | 需登录 |
| `GET /api/daily-retention` | 留存/解散率趋势（支持hall） | 需登录 |
| `GET /api/weekly-report` | 周报数据（支持hall） | 需登录 |
| `GET /api/hall-stats` | 大厅统计（按角色过滤） | 需登录 |
| `GET /api/detail-table` | 明细表格（支持hall） | 需登录 |
| `GET /api/alerts` | 预警列表 | 需登录 |
| `POST /api/uid-query` | UID查询 | 需登录 |
| `GET /api/search-suggest` | 搜索建议 | 需登录 |

---

## 十一、Cookie来源说明

- **UID查询Cookie** → `server1.tuwan.com:10010`（队长数据查询）⚠️ 当前已过期
- **数据抓取Cookie** → `bigdata.tuwan.com`（姐妹团统计数据）
- **厅运营Cookie** → `http://server1.tuwan.com:10010/fly2013/play_chatroom.php`（厅运营UID抓取）

---

## 十二、下次会话建议

1. 逐页定稿：厅分析 / 姐姐分析 / UID查询 三页预览最后过一遍，确认口径（尤其 UID 页 `team_info` 妹妹流水口径、画像表存活率）
2. 重测占位值：持续率阈值 65%、妹妹成长封顶 T=30（用真实数据跑分布）
3. 定培养力总分 v2 两个新因子口径（毕业后留存 / 姐姐流水牌子）
4. 收口后按「统一落地清单」一次性改写 backend/app.py + frontend/*
