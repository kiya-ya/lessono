# 姐妹团看板系统 — 项目上下文记忆文档

> 生成时间：2026-08-12
> 项目路径：`D:\姐妹团看板系统`
> Git最新提交：`69bcd40` (157次提交)

---

## 一、项目概述

**姐妹团数据统计报表** — 面向「点点开黑」平台运营团队的Web数据看板，追踪姐妹团（姐姐+妹妹配对）的成团、留存、解散、流水等核心指标，支持政策上线效果追踪。

### 技术栈
- **后端**：Python 3 + Flask + SQLite
- **前端**：原生 HTML/JS + ECharts（本地文件）
- **数据抓取**：Python爬虫（BeautifulSoup + Requests）
- **部署**：本地运行 / Docker
- **认证**：Cookie-based（auth_uid）+ 验证码登录

---

## 二、数据库结构（stats.db）

| 表名 | 说明 |
|------|------|
| `users` | 用户白名单（uid, nickname, role: admin/hall_manager） |
| `hall_managers` | 厅运营UID与大厅对应关系（172条记录） |
| `team_detail` | 姐妹团明细数据（最新快照） |
| `weekly_report` | 周报汇总数据（按大厅+周维度） |
| `stats_daily` | 每日统计数据 |
| `hall_stats` | 大厅级统计快照 |
| `trend_data` | 趋势数据 |
| `alerts` | 预警记录 |
| `crawl_log` | 抓取日志 |

### 关键数据
- 管理员UID：`34315471`（role=admin）
- 厅运营数量：约40+人（role=hall_manager）
- 大厅总数：约155个（有姐妹团的约152个）
- 厅运营平均管理：3-5个厅

---

## 三、已完成的功能

### 核心模块
1. **📊 周报概览** — KPI卡片（新成团、进行中、留存率、解散率、礼物奖励金额、主动解散占比）
2. **📈 核心趋势** — 留存率/解散率/礼物流水/任务活跃度 四张趋势图
3. **🔍 对比分析** — 本周vs上周指标对比表 + 各大厅数据对比柱状图 + 新成团vs解散双轴图
4. **📋 明细数据** — 姐妹团明细表格，支持搜索/排序/分页/状态筛选
5. **👤 UID查询** — 查询队长UID数据，含本周vs上周对比、姐妹团参与明细、姐姐vs妹妹对比

### 登录与权限
6. **🔐 登录页面** — 深色渐变背景 + 粒子动效 + iOS玻璃卡片 + 字母验证码
7. **权限系统** — 白名单制（admin / hall_manager 两种角色）
8. **Cookie管理** — 双Cookie分别管理（UID查询Cookie + 数据抓取Cookie）

### 厅运营视角（最新完成）
9. **大厅下拉框** — 厅运营登录后只显示自己管理的厅 + 「所有大厅」选项
10. **对比分析过滤** — 厅运营默认只看自己管理的厅；选择「所有大厅」可看全平台
11. **数据隔离** — `/api/halls` 和 `/api/hall-stats` 均按角色过滤

### 数据导出
12. **PDF导出** — 周报概览+核心趋势+预警（reportlab）
13. **CSV导出** — 周报数据、明细数据、UID查询结果

### 自动化
14. **定时任务** — `daily_crawl.py` 每日自动抓取数据（需有效Cookie）
15. **数据更新** — 自动写入 stats.db 并更新 last_update.json

---

## 四、当前存在的问题

### ⚠️ 待验证/待测试
1. **后端重启问题** — 厅运营视角的代码修改已提交，但用户尚未重启后端验证是否生效
2. **下拉框默认值** — 厅运营默认选中第一个厅的逻辑需确认是否正确执行

### ⚠️ 已知Bug
3. **解散率始终为0** — 修复清单第2项，待实现：`api_kpi` 动态计算解散率
4. **明细数据权限** — 厅运营选择「所有大厅」时，明细数据是否仍受权限限制需确认
5. **数据准确性** — 累计流水 vs 当周礼物流水的数据来源需持续核对
6. **留存率公式** — 是否超过100%的边界情况需监控

### ⚠️ 待优化
7. **前端框架** — 目前为原生HTML/JS，用户曾询问是否需要迁移到Vue/React，暂未实施
8. **测试文件清理** — `debug_uid_*.html` 等测试文件需统一管理和一键清理
9. **Docker启动** — 有时启动超时，需检查容器配置
10. **Cookie过期** — 定时任务因Cookie失效而失败，需定期更新

---

## 五、待办事项（按优先级）

| 优先级 | 事项 | 状态 |
|--------|------|------|
| 🔴 高 | 重启后端并验证厅运营视角功能 | 待用户操作 |
| 🔴 高 | 修复解散率始终为0的问题 | 未开始 |
| 🟡 中 | 明细数据权限控制（运营看全部大厅时的行为） | 未开始 |
| 🟡 中 | 测试文件清理脚本（clean_tests.bat修复） | 未开始 |
| 🟢 低 | 前端框架迁移评估（Vue/React） | 已讨论，未实施 |
| 🟢 低 | 数据准确性持续核对 | 持续进行 |

---

## 六、关键文件说明

```
D:\姐妹团看板系统
├── backend/
│   └── app.py              # Flask主入口（~1523行，含所有API路由）
├── frontend/
│   ├── index.html          # 主页面（看板）
│   ├── login.html          # 登录页（验证码+粒子背景）
│   └── js/
│       ├── api.js          # API请求封装（loadHalls/loadWeeks/loadDetailTable等）
│       ├── app.js          # 页面交互逻辑（onHallChange/onWeekChange/switchTab等）
│       ├── render.js       # 图表渲染（ECharts）+ UID结果渲染
│       ├── auth.js         # 认证相关
│       ├── utils.js        # 工具函数（getHallParam/getWeekParam）
│       └── config.js       # 全局配置
├── crawler/
│   ├── crawler.py          # 主爬虫（bigdata.tuwan.com）
│   ├── uid_crawler.py      # UID查询爬虫（server1.tuwan.com:10010）
│   ├── hall_manager_crawler.py  # 厅运营UID抓取
│   ├── db.py               # 数据库操作封装
│   └── daily_crawl.py      # 定时任务入口
├── data/
│   └── stats.db            # SQLite主数据库
└── start.py / app.py       # 启动脚本
```

---

## 七、近期Git提交记录

```
69bcd40 fix: 厅运营视角 - api_halls按角色过滤, api_hall_stats按运营身份过滤, 对比分析联动hall参数
8e0a656 fix: 删除旧版api_halls重复定义，确保厅运营只能看到管理的厅
01b5ea8 feat: 厅运营登录后大厅下拉框功能
4e344eb fix(api_kpi): relax data check - only error when both daily and weekly are empty
29cdcfb fix: 删除api.js中残留的旧版loadWeeks代码块
e0f3b9b feat: 周选择器动态添加本周选项
cd7651f fix: 修复render.js中重复函数定义导致的页面空白
81617de fix: 当前周期显示和UID等级柱状图修复
191b3d3 fix: 修复登录页脚本崩溃，添加退出登录功能
```

---

## 八、接口清单

| 接口 | 说明 | 权限 |
|------|------|------|
| `GET /api/check-auth` | 检查登录状态 | 公开 |
| `POST /api/login` | 登录（UID+验证码） | 公开 |
| `GET /api/captcha` | 获取验证码图片 | 公开 |
| `GET /api/halls` | 大厅列表（按角色过滤） | 需登录 |
| `GET /api/kpi` | KPI概览 | 需登录 |
| `GET /api/trends` | 趋势数据 | 需登录 |
| `GET /api/daily-retention` | 留存/解散率趋势 | 需登录 |
| `GET /api/weekly-report` | 周报数据 | 需登录 |
| `GET /api/hall-stats` | 大厅统计（按角色过滤） | 需登录 |
| `GET /api/detail-table` | 明细表格 | 需登录 |
| `GET /api/alerts` | 预警列表 | 需登录 |
| `POST /api/uid-query` | UID查询 | 需登录 |
| `GET /api/search-suggest` | 搜索建议 | 需登录 |

---

## 九、Cookie来源说明

- **UID查询Cookie** → `server1.tuwan.com:10010`（队长数据查询）
- **数据抓取Cookie** → `bigdata.tuwan.com`（姐妹团统计数据）
- **厅运营Cookie** → `http://server1.tuwan.com:10010/fly2013/play_chatroom.php`（厅运营UID抓取）

---

## 十、下次会话建议

1. **首先确认后端已重启**，然后用厅运营账号测试下拉框和对比分析
2. 如果还有问题，检查浏览器F12控制台的 `/api/halls` 请求响应
3. 继续进行解散率修复和明细数据权限优化
